import 'package:flutter/material.dart';

class InstructorProfileWidget extends StatelessWidget {
  const InstructorProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
